import * as React from 'react';
import './Settings.css';

export interface IProps {
}

export default class Settings extends React.Component<IProps, any> {
    render() {
        return (
            <div>
                Settings... (drop down with ligaauswahl and editable line for site price fetch selection)
			</div>
        );
    }
}